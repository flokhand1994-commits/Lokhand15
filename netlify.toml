# Static HTML only — no build. Publish the repo root.
# If deploys fail with "Build image no longer supported":
# Netlify → Site configuration → Build & deploy → Build image → pick the current default (e.g. Ubuntu 24.04).
[build]
  publish = "."
